kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: redis-hdd
  annotations:
    volume.beta.kubernetes.io/storage-class: "hostpath"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: mail-storage
  annotations:
    volume.beta.kubernetes.io/storage-class: "hostpath"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 3Gi
