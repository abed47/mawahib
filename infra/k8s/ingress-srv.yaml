apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-srv
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/use-regex: 'true'
    nginx.ingress.kubernetes.io/keep-alive: 'true'
spec:
  rules:
    - host: mawahib-abed.com
      http:
        paths:
          - path: /api/v1/u
            pathType: Prefix
            backend:
              service:
                name: users-srv
                port:
                  number: 4001